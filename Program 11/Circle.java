
class Circle {
    private double radius;

    public Circle(double radius) {
        this.radius = radius;
    }

    // toString method to represent Circle object
    @Override
    public String toString() {
        return "Circle [radius=" + radius + "]";
    }
}